# 已确定的决策

更新日期：2026-09-10。依据已执行需求及用户提供的开发上下文方案。实现状态与规划分开记录；规划条目不代表本次授权开发该功能。

## 当前约束与已实现行为

1. 一级模块只有健身房地图、器械库；品牌管理在器械库内部，16 个应用标签作为标签，不作为一级模块。
2. 器械顶级分类固定为固定器械、有氧器械、自由力量、龙门架。品牌和搜索适用于全部类型；部位、16 个应用标签和插片／挂片仅适用于固定器械，自由力量仅细分哑铃、杠铃、史密斯、力量架。
3. 固定器械部位和应用标签保留多选。新建必须主动选顶级分类及适用子字段，切换类型清除不适用字段。旧中文 `body`、`applicationTag` 和既有 `part`、`parts`、`tags` 保持兼容；可可靠识别的旧分类迁为固定器械，未知记录标记待核实，保留原始信息，不靠名称猜测强制入类。
4. 场馆与器械为多对多关系。场馆进入器械库看图、多选、批量关联；已有关联可更新，批量写入必须完整校验。
5. 场馆可关联具体品牌标签，与普通标签并列显示；点击进入对应品牌器械库，并清除妨碍跳转结果的旧器械筛选。
6. 使用外部测评系统；通过 JSON（JavaScript Object Notation，JavaScript 对象表示法）导入摘要、原始内容及链接。没有明确评分时显示待补充，不补造评分。
7. 程序目录与长期数据目录分离。升级、迁移、备份恢复必须保护用户记录和图片，旧版数据继续可读。
8. 品牌可以没有官网；官网读取结果由用户确认分类后保存。读取失败允许手动录入和本地图片。
9. 当前为个人本地桌面程序；多人共享、账号和小程序属于后续独立范围，不能将本地运行等同于多人同步。
10. 批量官网导入先保存独立候选池；分类规则仅提供建议，用户确认后再入正式库。重复优先按品牌和型号识别，再按品牌和规范名称提示；默认不重复创建。补充已有记录仅填空字段，不覆盖非空名称、图片或场馆关联。
11. 旧库结构迁移前保留完整可重连目录快照并校验；旧压缩备份恢复保留原始压缩文件。已有缺项记录继续可读及修改非分类字段，新建不能以待核实状态绕过分类校验。
12. 网站拒绝访问、限流和失败需给出真实状态及辅助导入路径，不绕过验证码或安全验证。文档单页导入维持现有流程，批量文档和自动文字识别不在此次范围。

## 后续方向，尚未实现

附件明确列为长期方向，保留在此，不当作现有功能或立即执行任务：

- 地图按尺度分层：全国省级密度 → 省内城市密度／热力 → 城区热力图 → 局部聚合 → 最终单点标记。具体阈值、统计口径与交互需在对应实现任务中明确。当前地图主要使用行政区轮廓和场馆标记。
- 场馆主题色的更广编辑和详情展示：现有悬停浮卡已读取场馆颜色，整体地图保持统一视觉；更广的编辑与展示尚未实现。

## 决策维护

已确定的决策直接沿用；用户明确改变方向时更新对应条目，必要时用一句话注明替代原因。示例版本、候选方案和未来功能不得写成已发布事实。实现前同步相关决策，实现后同步 `TODO.md` 的状态；不将讨论全文复制到这里。


## 1.4.0 公共资料与版本发布

已确认公开源码、软件安装版和场馆/器械基础资料及选定图片。采用维护者发布、订阅者预览后更新、通过仓库问题单提交修改建议的模式。个人打卡、私人备注、原始测评、设置与批量导入草稿不公开；资料同步先备份，保留本地修改与冲突，不删除上游移除的本地记录。程序更新和数据更新分开进行。
