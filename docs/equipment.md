# 器械分类与品牌目录批量导入

更新日期：2026-09-10。对应版本 1.3.0。源码回归、真实品牌目录验证及打包程序复核均已完成，完整安装包位于 release/NationalGymMap-Setup.exe。

## 分类与档案

| 顶级类型 | 保存值 | 适用子字段 |
| --- | --- | --- |
| 固定器械 | `fixed` | 必选部位及对应应用标签；可选插片／挂片 |
| 有氧器械 | `cardio` | 无更深分类 |
| 自由力量 | `free_weight` | 必选哑铃 `dumbbell`、杠铃 `barbell`、史密斯 `smith` 或力量架 `rack` |
| 龙门架 | `cable_station` | 无更深分类 |

固定器械保持多部位、多标签，不将旧多选信息压缩为单值。胸对应推胸、夹胸；肩对应肩推、中后束；背对应背下拉、背后拉；腿对应近固蹬、远固蹬、髋伸、髋旋、腿屈伸、腿弯举；核心对应脊柱屈、脊柱伸；手臂对应二头、三头。

手动新增和单页导入需要主动选择类型。切换类型时清除不适用部位、标签、负重类型或自由力量子类。器械名称、型号、品牌、图片、来源、备注及原有场馆关联独立保留。来源字段包括 `sourceType`、`sourceUrl`、`sourceDate`，核实字段包括 `verificationStatus`、`verifiedBy`、`verifiedAt`，同时保存创建和更新日期。`productUrl` 和 `imageSource` 继续保留兼容。

## 候选工作台

选择品牌并扫描官网目录或系列页，先生成候选，随后顺序读取产品详情。队列控制并发和请求间隔，单页失败不会终止整个批次。候选中保留图片来源、名称、型号、系列、描述、官网分类、建议分类及错误；规则建议必须由用户复核。扫描和编辑候选不写入正式器械集合。

从 1.5.1 起，扫描、读取详情、编辑和预览只保留临时草稿；关闭、新扫描或切换历史时释放，未确认草稿不写入数据库。候选可逐条或批量删除，无需先标记忽略；已导入器械不受候选删除影响。确认导入成功后才把器械与批次写入数据文件的 `importBatches`。历史批次仍可查看，只有已确认保存的历史参与后续暂缓和忽略记忆。

最终确认前展示新增、补充、重复、暂缓、忽略、失败及待下载图片数量。品牌与型号优先识别确定重复，品牌与规范名称提示疑似重复，来源辅助匹配；同批候选也参与查重。用户可补充已有记录、作为新器械或忽略。补充默认仅填空字段，原有非空名称、图片和场馆关联保留。确认后整批校验并原子保存，图片下载失败仍允许保留基本档案并记录图片失败状态。

## 官网错误与图片

响应保留状态和原因，区分拒绝访问、页面不存在、限流、服务异常、超时、网络错误及解析失败。HTTP（Hypertext Transfer Protocol，超文本传输协议）403 不等于永久无法读取，也不能当作成功；辅助浏览器和 PDF（Portable Document Format，便携式文档格式）单页导入是独立兜底。不会自动绕过验证码、安全挑战或访问限制。自动文字识别和批量文档导入不在此次范围。

图片按文件内容识别并存入本地图片目录。此次增加 AVIF（AV1 Image File Format，AV1 图像文件格式）内容识别，核对文件类型盒中的品牌标识，不依赖文件扩展名或任意正文匹配，减少官网现代图片格式被错误拒绝。

## 旧数据迁移和还原

保留 `schemaVersion: 1`，扩展标识为 `metadata.equipmentModelVersion: 1`。读取未迁移数据库时，旧部位代码或中文胸、肩、背、腿、核心、手臂转换为现有部位代码；兼容旧 `body`、`applicationTag` 及 `part`、`parts`、`tags`。有可靠部位或标签的记录识别为固定器械，无法判定则保留未分类并标记 `needs_review`。旧缺项档案允许加载及修改非分类字段；主动改分类时需要满足新规则。原记录标识、图片引用、品牌、链接和场馆关联不变。

迁移前复制完整数据目录到 `backups/equipment-model-*`，排除已有备份，逐文件校验哈希成功后才替换正式数据库；快照路径记录在 `metadata.equipmentMigrationBackup`。磁盘不足、复制失败或校验失败时不会写入迁移后数据库。

需要还原目录快照时，先复制该完整快照至一个新的独立数据目录，再通过程序的重新关联流程选择它；当前数据可用时可在独立配置启动中关联该目录，避免覆盖当前档案。关联后当前程序会再次执行兼容迁移并保留新的迁移前快照。备份目录中的原始文件可供旧版程序读取，不必手改记录。

恢复旧压缩备份时，先验证清单和全部文件，再保存当前目录的安全备份，并将原始压缩文件额外复制到 `backups/equipment-model-source-*.zip`。该原始压缩文件可通过正常恢复备份操作重新使用；恢复失败保留当前数据并回退。

## 验证记录

### 1.5.2 搜索分类及品牌系列

器械库顶部类型计数仅随品牌变化，搜索/系列/部位等条件只改变结果数量。选择胸部后出现全部、推胸、夹胸二级按钮；品牌条件保持有效。手臂增加小臂，腿增加小腿，现有肩推显示为推肩并保留旧存储键。

固定器械的其他部位可手动添加并复用，例如颈部。仅有自定义部位可不填应用标签，与内置部位混选仍需匹配标签。单台器械可填写一个系列，建议来自当前品牌已保存器械，切换品牌清空系列；空系列不显示占位。系列进入详情、卡片、搜索、筛选和共享同步。表单新输入限制 100 字，历史导入较长系列仍可读取和原样保存。

综合搜索匹配名称、型号、系列和品牌，亦可选仅名称/型号/系列。场馆已关联列表与从器械库选取窗口共用相同规则；前者限本场馆，后者查全库且筛选不清除已勾选项目。向下滚动一屏后可返回列表顶部，保留所有条件。

本轮 65 项单元测试、7 组新增桌面场景、2 组表单界面、3 组既有分类、6 组批量导入、6 组完整桌面回归通过，包含实际重启、备份恢复、系列共享冲突保留本地修改。没有生成 1.5.2 安装包。

### 1.5.1 网站图片与浏览器采集

单条及批量网站导入展示列表页对应条目 1 张和详情页最多 3 张候选图片（按来源链接去重），标注列表/详情来源并默认选列表图，选择其中一张作为封面，或选择不使用网站图片。候选预览不写正式器械或图片；点击保存/确认导入后才保存选中图片。单条图片保存失败会保留表单并提示重试；全部预览图片失败时仍可保存文字资料。批次确认失败会清理本次新图片，批次历史不内嵌候选图片。旧有本地手工图片上传流程保持不变。

应用内浏览器仍无法正常读取的页面，可使用项目内 `browser-capture/` 扩展，在已正常打开的普通浏览器页面主动采集。扩展将目录详情和每台器械最多四张候选图片装入一个采集文件；应用内选择“导入浏览器采集文件”，不需要逐张保存上传。采集原图独立保留至关闭草稿，不受关闭辅助浏览器影响。浏览器安装方法见 `browser-capture/README.md`。

采集文件最多 50 页、每页详情图最多 3 张，带有对应列表来源时另加 1 张列表图、解码总量 48 兆字节，文件不超过 64 兆字节；受限或失败页面明确记录。此路径要求普通浏览器已能正常访问页面，不能保证网站拒绝访问时仍成功。Full Circle Padding 尚无实站整批成功证据。

本轮验证：60 项单元测试、19 组本地网页与离线采集导入回归、5 组图片选择界面、6 组批量桌面回归，以及真实浏览器扩展在本地多层目录/背景图片/配件过滤下的采集通过。以下旧版真实网站及打包记录仅属于当时版本；本轮未打包。

`node --test tests/equipment-model.test.cjs tests/catalog.test.cjs tests/storage.test.cjs`：20 项通过，覆盖四种分类、子类必填、类型切换清理、旧中文映射、原记录与图片关系保留、迁移幂等、目录快照重新关联、旧压缩备份恢复、批次重启持久化及既有目录业务。

整合验证：全部 34 项单元测试通过；新增四分类手动窗口测试 3 组、批量窗口测试 5 组、图片格式桌面导入测试通过。既有完整桌面业务 6 组、悬停 6 组、网页导入 15 组、文档导入 9 组，以及旧数据、多部位、目录备份恢复和跨盘迁移通过。

真实目录：https://bootybuilder.com/product-category/machines/ 。本次在独立临时档案发现 23 项，逐项详情读取成功 23 项，失败 0 项，空测试库重复 0 项；13 项无可靠分类规则，保持人工确认。正式器械集合保持 0 条，没有把测试候选写进用户档案。逐项证据见 test-results/batch-live.json。

AVIF（AV1 Image File Format，AV1 图像文件格式）通过本地编码样本验证：手动选择、官网图片下载、保留原始字节、桌面解码及备份恢复。内容识别参考 [开放媒体联盟规范](https://aomediacodec.github.io/av1-avif/v1.2.0.html)。

网络仍由站点和用户网络决定。Panatta 的拒绝访问限制未解除，不宣称跨网站万能抓取；辅助浏览器、单页文档和手工整理继续可用。目录每批上限 200 项，不自动遍历分页；扫描缩略图失败不阻止基础信息，正式图片失败标记待补图。


打包验证：包内 16 个后端与构建文件逐字节匹配当前代码；直接启动打包程序完成四分类、批量工作台、图片导入、网页及文档导入测试。验收元数据与安装包校验值见 release/verification.json。没有覆盖用户现有安装，也没有操作用户正式档案。

列表图必须与器械详情链接精确配对，不能使用相邻条目图片。单独粘贴详情链接时，仅尝试同站上一级目录并核对链接；目录不可用时仍保留详情结果。详情读取失败时保留已读取的列表名称及图片，并显示失败提示。选中哪张只保存哪张，候选不写正式数据库或图片目录。
