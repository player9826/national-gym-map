# 全国健身房地图

更新日期：2026-09-24。当前源码版本 1.5.3；最新版网页及完整公开资料已部署并通过线上验证，桌面最近公开安装包仍为 1.5.2。

## 定位与现状

2026-09-22：新增公开只读网页模式，地图、器械、筛选与详情复用现有界面；桌面继续管理本地资料。网站从审核后的共享快照构建，图片生成缩略图与详情图，独立发布且不升级桌面版本。部署和本轮验收状态以 `TODO.md` 为准；网页使用及发布流程见 `docs/web.md`。

面向个人长期使用的 Windows 本地桌面应用，管理健身房地图、训练足迹和器械档案。1.2.0 新增列表和地图共用的悬停浮卡、浏览器辅助及文档导入；安装包已构建，打包可执行程序使用隔离档案回归。此前覆盖安装验证属于 1.1.0；本轮不替换用户现有安装。尚无多人共享服务、账号系统或小程序。

一级模块只有健身房地图和器械库。已实现场馆档案、打卡、照片、地图选点与筛选；器械品牌管理、多部位选择、插片／挂片筛选；场馆品牌标签跳转、看图多选批量关联；外部测评导入、数据导入导出、备份恢复与目录迁移。

1.3.0 已完成器械四类分类、品牌目录批量导入工作台及 AVIF（AV1 Image File Format，AV1 图像文件格式）图片支持。器械分为固定器械、有氧器械、自由力量、龙门架。1.5.1 网站导入改为临时预览、最多三张候选图片，确认后才保存器械、选中图片和批次；新增普通浏览器采集文件入口，详细规则见 `docs/equipment.md`。用户后续已授权构建 1.5.1 本地安装包，构建及打包验证状态以 `release/verification.json` 为准；用户已进一步授权封装并公开发布 1.5.2。

技术栈：Electron 桌面框架、React 界面组件库、Leaflet 地图库、Vite 构建工具、Node.js 运行环境、Playwright 窗口自动化。数据采用 JSON（JavaScript Object Notation，JavaScript 对象表示法）文件与本地图片。

1.5.3 本地安装包内容：列表页 1 张加详情页最多 3 张候选图片；地图紧凑布局、器械库恢复较高卡片；仅使用全球离线底图。正式公开版仍为 1.5.2。

1.5.3 同时包含：场馆国家/地区选择及旧新加坡记录兼容；器械库每行四张、较高卡片，品牌标识独立显示、系列位于名称下方，名称最多两行省略；选中品牌可显示单独上传且横贯整行的展示图，点击查看公开介绍。新本地安装包已生成，尚未安装或公开发布。

## 上下文入口

1.5.2 完成品牌范围类型计数、部位二级筛选、小臂/小腿、自定义部位、品牌系列编辑与共享、字段搜索、场馆两处器械搜索筛选，以及返回顶部。新筛选逻辑由 `src/equipment-filtering.js` 和 `src/EquipmentFilters.jsx` 共同实现，三个界面共用；1.5.2 本地安装包已生成并通过包内容、更新清单和隔离档案桌面回归，已完成公开发布及在线下载核验。

- `ARCHITECTURE.md`：实际文件入口、数据关系、目录与验证方式。
- `DECISIONS.md`：已确定的约束与尚未实现的规划方向。
- `TODO.md`：当前任务、下一步、外部条件和最近验证状态。
- `README.md`：使用、开发、升级说明；`ACCEPTANCE.md`：详细验收记录。
- `docs/equipment.md`：器械四类字段、批量候选、重复处理和旧数据迁移还原方法。

## 更新原则

1. 新任务先读本文件和 `TODO.md`，再按任务读取相关决策、架构条目和代码；不默认扫描全项目或重读历史对话。
2. 先明确目标、修改范围、已有约束和验收标准，再直接执行已授权工作。方案已确定时不反复讨论；产品方向改变时先同步 `DECISIONS.md`。
3. 只做完成任务所需的局部修改，不整文件重写、不附带重构、不凭目录示例搬迁代码。
4. 默认针对相关行为验证。数据结构、进程间通信、迁移、打包、全局样式或跨模块变更时，按影响扩大回归范围。
5. 完成任务后更新 `TODO.md`；定位、版本或阶段改变时更新本文件；真实架构改变时更新 `ARCHITECTURE.md`；决策改变时更新 `DECISIONS.md`。只更新受影响段落，不累计聊天记录。
6. 正式发布时统一核对版本、构建和验收记录；日常文档或小修不自动新增版本。未运行的测试不得写为通过，规划不得写为已实现。
7. 完成后简要报告改动、文件、验证与遗留问题。所有专有名词缩写后用括号注明全称；代码标识符和文件路径保持原样。

## 重要约束

程序目录与长期数据目录分离；保留旧数据兼容，不用测试数据污染用户档案。品牌和应用标签不升级为一级模块。没有证据时不推断评分、训练部位或负重类型。
